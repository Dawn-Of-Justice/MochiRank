# Runtime-only deps for rank.py (the Docker rank image).
# The full requirements.txt additionally pulls torch / anthropic / streamlit /
# python-docx, which are used ONLY in the offline phase — they are deliberately
# excluded here to keep the rank image lean and provably network-free.

numpy>=1.26
scipy>=1.12
xgboost>=2.0
rank-bm25>=0.2.2
model2vec>=0.3        # torch-free static embedder (potion-base-8M) — built at runtime
flashrank>=0.3        # optional cross-encoder re-rank; rank.py degrades gracefully if absent
