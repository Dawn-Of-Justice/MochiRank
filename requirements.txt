# Runtime deps for the Streamlit demo (sandbox/app.py) and rank.py.
# Offline-only packages (anthropic, python-docx, sentence-transformers, shap)
# are intentionally excluded — they are only needed for the precompute phase.

# Core numerics
numpy>=1.26
scipy>=1.12
pandas>=2.1

# Streamlit UI
streamlit>=1.35
altair>=5.0

# Embeddings (torch-free; potion-base-8M vendored in artifacts/)
model2vec>=0.3

# BM25
rank-bm25>=0.2.2

# Learning-to-rank
xgboost>=2.0

# Cross-encoder reranker (Stage E — optional; downloads model on first run if not cached)
flashrank>=0.2.9
